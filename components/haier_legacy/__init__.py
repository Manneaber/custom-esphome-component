# Haier (legacy) external component for ESPHome.
#
# This packages the original HaierFlexisPlus.h "custom" climate class as a
# modern ESPHome external component, replacing the removed `platform: custom`
# lambda mechanism.
